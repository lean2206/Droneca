/* Esta función se encarga de crear HTML para agregar un producto a la interfaz. Se debe proporcionar el nombre, precio, modelo y la ruta de la imágen del producto
 */

function addProd(nombre, precio, modelo, img){
    
    img = `<img class=new-prod src="${img}">`;
    nom

}